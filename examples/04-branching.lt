program do
    remember that myVariable is a boolean
    myVariable is now FALSE

    if myVariable
        print "The variable contained FALSE. Wait, what?"
    else if 42 > 99 || 42 == 42
        print "42 is truly the answer"
    else
        print "This code does NOT make sense anymore"
    stop
stop
